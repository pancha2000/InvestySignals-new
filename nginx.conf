# ============================================================
#  Nginx config for InvestySignals
#  Save as: /etc/nginx/sites-available/investysignals
# ============================================================

server {
    listen 80;
    server_name YOUR_DOMAIN_OR_IP;   # ← ඔබේ domain/IP දමන්න

    # Redirect HTTP → HTTPS (SSL setup කළ පසු uncomment)
    # return 301 https://$host$request_uri;

    # Proxy all requests to Node.js backend
    location / {
        proxy_pass         http://127.0.0.1:2000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";    # WebSocket support
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;                   # WebSocket timeout
    }

    # Cache static files
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        proxy_pass http://127.0.0.1:2000;
        proxy_set_header Host $host;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}

# ── HTTPS (SSL) — Certbot run කළ පසු auto-generated ──────────
# server {
#     listen 443 ssl;
#     server_name YOUR_DOMAIN;
#     ssl_certificate     /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#     location / {
#         proxy_pass http://127.0.0.1:2000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_read_timeout 86400;
#     }
# }
